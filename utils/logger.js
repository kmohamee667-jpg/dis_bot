/**
 * Disables logging as requested by the user.
 * This function is now a no-op (does nothing).
 */
async function logAction(client, guildId, logData) {
    // Logging is disabled.
    return;
}

module.exports = { logAction };
